{
  "name": "chatgpt-conversation-extractor",
  "version": "0.2.0",
  "private": true,
  "description": "Local userscript for exporting the active branch of a ChatGPT conversation from same-origin JSON.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
